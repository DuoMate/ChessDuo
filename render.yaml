# render.yaml - Render Zero-Config Deployment
# Placed at repo root, points to server/ directory

services:
  - type: web
    name: chessduo-stockfish
    region: oregon
    plan: free
    rootDir: ./server
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production